<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Login_Form.RegisterPage">
    <ContentPage.Content>
        <StackLayout Padding="20">
         
            <Label Text="Register to"
                   FontSize="32"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   TextColor="#d40827"
                   FontAttributes="Bold"
                   ></Label>

            <Label Text="CHM Technology"
                   FontSize="32"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   TextColor="#d40827"
                   FontAttributes="Bold"
                   ></Label>
            <Image Source="arsenal.png" WidthRequest="120" HeightRequest="120"></Image>

            <Entry x:Name="TxtEmail" Placeholder="Email" TextColor="Blue" PlaceholderColor="gray" />
            <Entry x:Name="TxtPassword" Placeholder="Password" IsPassword="True" TextColor="BLue" PlaceholderColor="gray"/>
            <Entry x:Name="TxtConfirm" Placeholder="Confirm Password" IsPassword="True" TextColor="BLue" PlaceholderColor="gray"/>

            <Button x:Name="btn_signup" Text="Sign Up" BackgroundColor="Blue" TextColor="White"
                    Clicked="btn_signup_Clicked"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>